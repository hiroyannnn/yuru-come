///|
/// コア（lib）の公開 API をルートパッケージから直接使えるように再エクスポートする。
pub using @lib {
  type Comment,
  type Token,
  type BundleMetric,
  type BundleConfig,
  type BundleHit,
  type BundleSummary,
  type Bundler,
  type ReactionRules,
  default_reaction_words,
  type Kind,
  type Triage,
  type ResponseError,
  jev_kinds,
  priority_levels,
  build_request,
  parse_response,
  parse_response_text,
  participant_id,
  normalize,
  normalize_tokens,
  unify_width,
  compress_runs,
}
